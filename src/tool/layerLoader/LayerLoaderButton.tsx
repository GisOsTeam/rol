import * as React from 'react';
import { IFunctionBaseWindowToolProps } from '../BaseWindowTool';

export function LayerLoaderButton(props: IFunctionBaseWindowToolProps) {
  return <span>LayerLoader</span>;
}
